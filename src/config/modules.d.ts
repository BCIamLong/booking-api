// * this file is not used because somehow the declare not work maybe we need to create the file in the root level like maybe in src
// import { IGuest, IUser } from '~/api/interfaces'

// declare module 'express-serve-static-core' {
//   // * for authentication and authorization purposes
//   interface Request {
//     user: Omit<IUser, 'passwordConfirm'> | Omit<IGuest, 'passwordConfirm'>
//   }
// }
